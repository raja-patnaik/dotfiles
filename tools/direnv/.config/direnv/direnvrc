# direnv global configuration
# https://direnv.net/

# Colorize log output
export DIRENV_LOG_FORMAT=$'\033[36mdirenv: %s\033[0m'

# Load .env file if present
dotenv_if_exists() {
  local env_file="${1:-.env}"
  [[ -f "$env_file" ]] && dotenv "$env_file"
}
